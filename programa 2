#include <stdio.h> 
int main () {
char nome1[30], nome2[30];
int idade1, idade2 ;
printf("Digite nome e idade:");
scanf("%s %d %s %d", nome1, &idade1, nome2, &idade2);
if (idade1>idade2)
{
    printf("%s", nome1);
}
    else
    {
        printf("%s", nome2);
    }
}
